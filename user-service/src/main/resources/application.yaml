spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASS}
  application:
    name: user-service


jwt:
  secret: ${JWT_SECRET}
  expiration: 8460000

server:
  port:
    8081